# ASMRTube 作業報告書

## 2026-08-29 v2.0 他プロジェクト設計再利用・Product Shell強化

### 目的

ASMRTube単体で新しいUIを一から考えるのではなく、自分のGitHub上で既に時間をかけて改善している `LyricTube / Lineup Lab / VReview` の良い設計をASMRTube向けに再利用し、操作性・分かりやすさ・データ保護・スマホ利用・見た目の完成度をまとめて引き上げる。

### 参考にした自分のプロジェクト

#### LyricTube

- モバイル用ドロワーサイドバー
- 設定画面
- 起動画面設定
- コンパクト表示
- サムネイル表示切替
- 動きを減らす設定
- ヘルプ導線
- モバイルダイアログの操作性

#### Lineup Lab

- 1px枠線を増やしすぎず背景差・影・余白で階層を作るUI
- 画像を主役にしたカード表示
- データ管理を独立した操作として見せる設計
- 重い常時アニメーションを避ける方針

#### VReview

- Dashboardの数値カード
- Status / Confidenceの見せ方
- 自動処理結果をユーザーが確認してから使う設計

### 今回変更した内容

- ASMRTubeのサイト表示をv2.0へ更新
- サイトアイコン `favicon.svg` を追加
- v2.0用CSSを `ui-base-v2.css / product-v2.css` へ分離
- JavaScriptへ見た目のCSSを大量に埋め込む構造を減らした
- 左ナビゲーションへ `概要` を追加
- ライブラリ概要Dashboardを追加
- Dashboardへ登録ASMR数を表示
- Dashboardへお気に入り数を表示
- Dashboardへ睡眠向け数を表示
- Dashboardへ総タイムスタンプ数を表示
- 最近聴いたASMRをサムネイルカードで表示
- 履歴がない場合は最近追加したASMRを表示
- よく使うタグと件数を表示
- Dashboardからお気に入り / 睡眠向け / 最近聴いたへ移動可能にした
- DashboardからASMR追加 / データ管理を開けるようにした
- 設定画面を強化
- コンパクト表示設定を追加
- サムネイル表示 / 非表示設定を追加
- 動きを減らす設定を追加
- 起動時に概要を表示する設定を追加
- 既存の明るさ30〜100%設定を維持
- 左上へ `?` ヘルプボタンを追加
- ヘルプへ基本操作 / タイムスタンプ例 / バックアップ注意 / ショートカットを追加
- `Ctrl + K` で検索欄へ移動するショートカットを追加
- 入力欄外で `?` を押すとヘルプを開けるようにした
- 900px以下で左サイドバーをドロワー化
- スマホで `☰` からサイドバーを開くよう変更
- `× / 背景 / Esc` でスマホサイドバーを閉じられるようにした
- スマホでASMR / 表示項目選択後にサイドバーを自動で閉じるようにした
- スマホ上部の操作ボタンを横スクロール化
- スマホのタグ一覧を横スクロール化
- スマホダイアログの操作ボタンを下部へ固定しやすい構成へ変更
- `safe-area-inset-bottom` を考慮
- `データ管理` ダイアログを追加
- ASMR / タイムスタンプ / プレイリスト / 保存サイズを表示
- データ管理から既存JSON書き出し / 読み込みを実行可能にした
- ブラウザ内ローカルスナップショット機能を追加
- JSON読み込み前に現在状態を退避する処理を追加
- ASMR削除前に現在状態を退避する処理を追加
- スナップショット復元前の状態も別キーへ一時退避するようにした
- v1.9パーサーの解析統計を取込画面へ表示
- `検出 / 見出し / 親 / 子 / 二言語 / URL時刻 / 要確認` を表示可能にした
- 各解析行へ `高 / 中 / 確認` のConfidence表示を追加
- 推定を含む行は保存前に気づきやすい見た目へ変更
- v1.9のタイムスタンプ解析ロジック自体は変更せず維持

### 新規ファイル

- `favicon.svg`
- `ui-base-v2.css`
- `product-v2.css`

### 変更したファイル

- `index.html`
- `ui-enhancements.js`
- `timestamp-parser.js`（v2.0シェルのキャッシュ更新のみ。解析ロジックは変更なし）
- `README.md`
- `作業報告書.md`

### 保存仕様

既存:

```text
asmrtube.library.v1
asmrtube.settings.v1
asmrtube.timestamp.view.v1
```

追加:

```text
asmrtube.snapshot.v1
asmrtube.snapshot.beforeRestore.v1
```

スナップショットは同じブラウザのlocalStorageへ保存するため、サイトデータ削除への対策にはならない。重要データは従来どおりJSON書き出しを推奨する。

### 崩していない仕様

- YouTube IFrame Player再生
- GitHub Pages静的構成
- YouTube Data APIキー不要
- `asmrtube.library.v1` のライブラリ形式
- 既存タイムスタンプデータとの互換性
- v1.9汎用タイムスタンプ解析
- コメント再取り込み時の同時刻置換
- A-Bリピート
- スリープタイマー
- お気に入り / 最近聴いた / 睡眠向け / プレイリスト
- JSON書き出し / 読み込み
- LyricTubeの保存データと分離

### 確認したこと

- GitHub上の `LyricTube / Lineup Lab / VReview` のREADME・構成・関連CSS/JSを確認してから移植内容を決定した
- 新しいCSSを `index.html` から読み込む構成へ接続した
- `timestamp-parser.js` はv1.9解析ロジックを維持し、v2.0のUIシェルだけを新しいキャッシュバージョンで読み込む構成にした
- READMEへ新しい保存キー・ファイル構成・注意点を記録した

### 確認できなかったこと

- 実ブラウザでDashboardの全ボタンを押した動作
- 設定ON/OFFの全組み合わせ
- スナップショットからの実復元
- iPhone / Android実機のドロワー操作
- 各スマホ幅でのネイティブdialog最終表示
- v2.0追加後の長時間連続再生

GitHub Pagesのビルド成功は別途確認するが、ビルド成功だけではJavaScriptの全操作確認にはならない。

### 今後必要な作業

- 実際の画面スクリーンショットを見ながらDashboard密度を微調整
- v1.9の `role / parentTime / subtitle` を通常タイムスタンプ一覧でもさらに視覚的に階層表示する改善
- タイムスタンプ内検索 / 種類絞り込みの検討
- 必要ならLyricTube同様にテーマ / アクセント色設定を追加
- スマホ実機での最終調整

## 2026-08-29 v1.9 汎用タイムスタンプ解析・回帰テスト追加

### 今回変更した内容

- YouTube公式の動画チャプター仕様と、実際のYouTubeコメント / 既存タイムスタンプパーサーで使われる一般的な記法を調査
- 書式ごとに正規表現を追加する方式から、複数段階の汎用解析方式へ `timestamp-parser.js` を再設計
- `m:ss / mm:ss / h:mm:ss` に加えて2時間以上の時刻を継続対応
- `(00:00)` / `(00:00:00)` / `【00:00】` / `[00:00]` 等の括弧形式へ対応
- `00:00 - タイトル` / `00:00-タイトル` 等の区切り差を吸収
- Markdownタイムスタンプリンクを解析
- YouTube URLだけが貼られた場合、`t` / `start` から秒数を取得するフォールバックを追加
- `t=90` / `t=90s` / `t=1m30s` / `t=1h2m3s` を解析可能にした
- `▷ / ▶ / ► / ▸ / ▹ / ▻ / ➤ / ➜ / ⏵` を親候補として認識
- `┗ / └ / ├ / ↳ / ↪ / ╰ / ┖ / ┕` を子候補として認識
- 大見出し → 親タイムスタンプ → 子タイムスタンプの3層構造を任意メタデータとして保持
- `└` だけが途中にある形式では、直前の通常タイムスタンプを親候補として関連付け
- 従来の `耳ふー → 右 / 左` 形式も維持
- 1行に潰れたコメントの2個以上の空白 / 改行による論理区切りを維持
- YouTubeカスタム絵文字の `yt3.googleusercontent.com` / gaming emoji Markdownを通常ラベルから除外
- 行が絵文字リンクだけの場合は表示名をフォールバックとして残す
- 日本語 + 英語の二言語ラベルを `label / subtitle` に分離
- `タオルとんとんTowel Tapping` のように空白がない二言語表記にも対応
- ASMRタグ判定へ英語表現を追加
- 同一ラベル・同一構造で時刻差が±1秒以内の重複を整理
- 判定結果へ任意の `role / parentTime / parentLabel / confidence / sourceStyle / subtitle` を追加
- 曖昧なものを無理に階層化せず、通常タイムスタンプとして残す方針へ変更

### 調査で確認したこと

YouTube公式の動画チャプターは、説明欄にタイムスタンプとタイトルの一覧を置き、以下が条件。

- 最初のタイムスタンプは `00:00`
- 3個以上
- 昇順
- 各チャプター10秒以上

ただしASMRTubeはコメント欄のユーザー作成タイムスタンプも対象なので、この公式条件は入力必須にはしていない。

既存のYouTubeチャプター抽出ツールでは、以下のような形式が一般的に扱われていることも確認。

```text
00:00 Title
(00:00:00) Title
02:00 - Title
```

YouTube URLの時刻指定には秒数だけでなく `1m30s` 等の形式も存在するため、URLフォールバック側で対応した。

### 回帰テスト

新規追加:

- `tests/timestamp-cases.json`
- `tests/timestamp-parser.test.html`

代表10形式を回帰ケースとして保存。

1. 通常Markdownタイムスタンプ
2. ハイフン区切り
3. 1行に潰れた `見出し → 右 / 左`
4. 大見出し + `▷親 / ┗子`
5. フラット + `└子` 混在
6. 日本語 + 英語
7. 括弧付き時刻
8. YouTubeカスタム絵文字混在
9. URLの `t / start` だけから時刻取得
10. 2時間超え

ローカルNode環境で同等ケースを実行し、上記10系統の主要解析結果を確認。`node --check` で `timestamp-parser.js` の構文エラーがないことも確認した。

GitHub Pages反映後は次のページでブラウザ回帰テスト可能。

```text
https://elitemay.github.io/asmrtube/tests/timestamp-parser.test.html
```

### 保存互換性

- `asmrtube.library.v1` 維持
- `asmrtube.settings.v1` 維持
- `asmrtube.timestamp.view.v1` 維持
- 旧 `{time,label,group,tags}` はそのまま読める
- 新しいフィールドはすべて任意
- 再取り込み時の同時刻置換仕様を維持

### 変更したファイル

- `timestamp-parser.js`
- `ui-enhancements.js`
- `index.html`
- `README.md`
- `作業報告書.md`
- `tests/timestamp-cases.json`（新規）
- `tests/timestamp-parser.test.html`（新規）

### 未確認 / 今後必要な作業

- GitHub Pages反映後の回帰テストページ10/10確認
- `role / parentTime` をタイムスタンプ一覧UIでさらに分かりやすく階層表示する改善
- 新しいユーザー作成形式が見つかった場合、先に回帰ケースへ追加してから解析ロジックを拡張
- iPhone / Android実機

## 2026-08-29 v1.8 タイムスタンプ表示モード再設計

### 今回変更した内容

- 長いタイムスタンプ一覧を常時すべて表示する方式を変更
- 右パネルへ `見出し / すべて` の表示切替を追加
- 初期表示を `見出し` モードに変更
- 親見出しを2列のコンパクトカードで一覧表示
- 親カードへ開始時刻と子タイムスタンプ件数を表示
- 親カードを押すと、その親の先頭へ再生位置を移動
- 選択した親の子タイムスタンプだけを下部へ表示
- 再生中の親が切り替わると見出しモード側も自動追従
- 親のないタイムスタンプ件数を `その他のタイムスタンプ` として表示し、押すと全表示へ切替
- `すべて` モードでは親グループを折りたたみ表示
- 親名クリックで開閉、親右側の再生ボタンで先頭へシーク
- 再生中の親だけ自動展開・強調
- 通常タイムスタンプ行を約30px基準へ圧縮
- 削除ボタンは通常時の主張を弱め、行ホバー時に見やすくした
- 表示モードを `asmrtube.timestamp.view.v1` へ保存
- ライブラリ / タイムスタンプ保存形式は変更していない
- 関連スクリプトのキャッシュバージョンをv1.8へ更新
- サイドバーのバージョン表示をv1.8へ更新

### 変更したファイル

- `timestamp-ui.js`
- `timestamp-parser.js`
- `ui-enhancements.js`
- `index.html`
- `README.md`
- `作業報告書.md`

### 崩していない仕様

- `asmrtube.library.v1` を維持
- `asmrtube.settings.v1` を維持
- タイムスタンプの `{time, label, group, tags}` 形式を維持
- YouTubeコメント解析ロジックはv1.7までの判定方式を維持
- 再取り込み時の同時刻置換を維持
- YouTube Data APIキー不要
- GitHub Pagesのみで動作する静的構成

### 未確認 / 今後必要な作業

- GitHub Pages反映後に `見出し / すべて` の切替が実ブラウザで正常か確認
- 長いASMRで親見出しが20件以上ある場合のカード一覧の見やすさ確認
- スマホ実機で1列表示へ切り替わることを確認
- 必要なら親カードの検索 / 種類別フィルタを追加

## 2026-08-29 v1.7 見出しジャンプ・親グループ操作改善

### 今回変更した内容

- タイムスタンプパネル上部へ「見出しジャンプ」バーを追加
- 親グループ名と親の開始時刻を横スクロール形式で一覧表示
- 親ボタンを押すと、その親の最初のタイムスタンプへYouTube再生位置を移動
- 同時にタイムスタンプ一覧も該当親ブロックまでスクロール
- 親グループ見出しそのものもクリック可能にし、同じ先頭ジャンプ操作へ統一
- 同名の親グループが複数存在する場合、開始時刻を併記して区別
- 再生位置が親グループ内にある場合、見出しジャンプボタンを自動ハイライト
- 再生中の親ブロック自体も強調表示
- 再生中の親が切り替わった時だけ、上部ジャンプバーを該当ボタンまで自動スクロール
- 親グループが存在しない動画では見出しジャンプバーを非表示
- タイムスタンプ保存形式は変更せず、既存の `group` データからナビゲーションを生成
- `timestamp-ui.js` と関連スクリプトのキャッシュバージョンをv1.7へ更新
- サイドバーのバージョン表示をv1.7へ更新

### 変更したファイル

- `timestamp-ui.js`
- `timestamp-parser.js`
- `ui-enhancements.js`
- `index.html`
- `README.md`
- `作業報告書.md`

### 崩していない仕様

- ライブラリ保存キー `asmrtube.library.v1` は変更していない
- 表示設定キー `asmrtube.settings.v1` は変更していない
- タイムスタンプ保存データ形式は変更していない
- YouTube Data APIキー不要
- GitHub Pagesのみで動作する静的構成
- コメント再取り込み時の同時刻置換仕様を維持
- 明るさ設定、A-Bリピート、スリープタイマー等の既存機能を維持

### 操作仕様

親グループが次のように存在する場合、

```text
耳ふー
  4:46 右
  5:38 左

綿棒
 10:48 右
 12:30 左

耳ふー
 14:34 左
 14:54 右
```

上部には次のようなジャンプ項目を表示する。

```text
耳ふー 4:46 | 綿棒 10:48 | 耳ふー 14:34
```

クリック時は「動画シーク + 一覧スクロール」を同時に実行する。

### 確認したこと

- 同名グループを名称だけでまとめず、連続ブロック単位で別チャプターとして扱う構成
- 親グループの終了時刻は、そのグループ直後にある次のタイムスタンプを境界として扱う
- 通常タイムスタンプ区間では親のアクティブ表示を解除する構成
- 親ジャンプUIは保存データを追加・変更せず表示時に生成する
- GitHub Pagesで利用可能な相対パス構成を維持

### 未確認 / 今後必要な作業

- GitHub Pages反映後の実ブラウザで親ジャンプをクリックし、シークとスクロールが同時に動作するか最終確認
- 長い動画で親グループが20〜30件以上ある場合の横スクロール操作性
- スマホ実機で見出しジャンプバーの幅・タップ操作確認
- 今後必要なら「親だけ表示するモード」「親グループの折りたたみ」も検討

## 2026-08-29 v1.6 タイムスタンプ見出し判定安定化

- 見出し候補の採用条件を厳格化
- 見出し候補の直後にある次タイムスタンプが `右 / 左 / 右耳 / 左耳` 等の子要素の場合だけグループ化
- グループ継続を連続する子タイムスタンプの間だけに制限
- タイムスタンプ表示をグループ単位の専用ブロックへ変更
- グループ終了後の通常タイムスタンプとの間に明確な区切りを追加

## 2026-08-29 v1.5 改行崩れコメント解析・重複修正

- YouTubeコメントをコピーした際に改行が潰れ、1行の中へ複数タイムスタンプと見出しが混在する形式へ対応
- コメント全体をタイムスタンプトークン単位で分解する方式へ変更
- 改行または2個以上の空白を論理的な区切りとして扱う処理を追加
- Markdownリンク内のURLを除去し、表示テキストだけを解析へ利用
- 再取り込み時、解析された時刻と同じ既存タイムスタンプを置き換えるように変更

## 2026-08-29 v1.4 タイムスタンプ構造修正・画面設定・UI品質改善

- 見出しを各ラベルへ文字列連結する方式を廃止し、任意の `group` として保持
- 画面全体の明るさ設定を追加
- 明るさを30〜100%で調整可能
- `asmrtube.settings.v1` へ表示設定を保存
- ヘッダーへ現在選択中作品のサムネイルを追加
- ブランドマーク、バージョン表示、パネルの階層感を調整

## 2026-08-29 v1.3 タイムスタンプ見出し保持

- タイムスタンプ直前の短い見出しを各ラベルへ連結する方式を追加
- Markdownリンク形式へ対応
- v1.4でグループ方式へ置き換え

## 2026-08-29 v1.2 登録自動化・タイムスタンプ操作性改善

- YouTube URL貼り付け時に動画タイトル / チャンネル名を自動取得
- YouTube oEmbedを第一候補、noembedをフォールバックとして使用
- タイムスタンプパネルをコンパクト化
- 長い一覧はパネル内スクロール
- 現在再生中のタイムスタンプを自動ハイライト

## 2026-08-27 v1.1 LyricTube UI寄せ

- ASMRTubeの画面構成をLyricTubeに近い構成へ変更
- 左サイドバー、中央プレイヤー、右タイムスタンプパネルの3カラム構成へ変更
- 配色、余白、角丸、ボタン、パネル、ダイアログ等をLyricTubeのデザインシステム基準へ変更

## 2026-08-27 v1 初期作成

- LyricTubeとは別の `EliteMay/asmrtube` リポジトリへASMRTubeを分離
- GitHub Pagesでルート公開できる静的構成を作成
- ASMRライブラリ、検索、タグ絞り込み、お気に入り、履歴、睡眠向け一覧、プレイリストを追加
- YouTubeコメント貼り付けによるタイムスタンプ抽出
- A-B区間リピート
- スリープタイマー
- JSONバックアップ / 復元
- YouTube動画IDによる重複登録防止