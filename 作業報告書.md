# ASMRTube 作業報告書

## 2026-08-29 v2.1 日常利用品質・安全性改善

### 今回変更した内容

#### 検索

- 左検索欄の対象を拡張
- タイトル / 配信者 / 作品タグだけでなく、以下も検索対象に追加
  - タイムスタンプ本文
  - 見出し `group`
  - 二言語副題 `subtitle`
  - 親名 `parentLabel`
  - タイムスタンプタグ
- 複数語はAND検索
- NFKC正規化と小文字化を行って検索
- 検索 / 絞り込みで0件になった時に真っ白な一覧を出さず、解除導線を表示
- お気に入り / 睡眠向け / 最近聴いた等が0件の場合は `すべて表示` への導線を表示

#### YouTube再生安定化

- YouTube IFrame API準備前に作品を選択した場合、その操作を予約
- API準備前に再生した場合も再生予約として保持
- Player準備後に最新の予約操作を反映
- cue済み作品を中央再生ボタンから再生した場合も `最近聴いた` へ記録
- Player未準備時の再生操作を無反応にせず、準備中Toastを表示

#### 削除安全性

- ASMR削除後8秒間 `元に戻す` を表示
- Undo時に以下を復元
  - ASMR本体
  - 元のライブラリ位置
  - プレイリスト内の位置
  - 最近聴いた内の位置
- 再生中作品を削除した場合はYouTube Playerを停止
- `currentId` / pending再生を初期化
- A-B区間リピートを解除
- v2.0の削除前ローカルスナップショットも併用

#### JSON読み込み安全化

- 読み込み前にJSON構造を検証
- `library` が無いJSONを拒否
- 動画ID / タイトルが無い壊れた項目を除外
- URLからYouTube動画IDを復元可能な場合は復元
- 同じYouTube動画IDの重複を除外
- 重複した内部IDは新しいIDを発行
- 不正なタイムスタンプ時刻を除外
- プレイリストの存在しない作品参照を除外
- `recent` の存在しない作品参照を除外
- 読み込み前にASMR / プレイリスト件数と除外件数を確認表示
- 確認後に現在ライブラリを置換
- 読み込み前スナップショットを維持

#### ライブラリ診断

`データ管理` に整合性診断を追加。

確認対象:

- IDなし
- タイトルなし
- YouTube動画IDなし
- 動画ID重複
- 不正タイムスタンプ時刻
- プレイリストの壊れた作品参照
- 最近聴いたの壊れた作品参照

診断は確認のみで、勝手な自動修復は行わない。

#### 親子タイムスタンプ表示改善

v1.9で解析・保存していた任意メタデータを、v2.1で表示にも使用。

- `role=parent` 行へ `親` バッジを表示
- `role=child` 行を `↳` と左ガイドで一段下げて表示
- `subtitle` がある二言語タイムスタンプは本文下へ小さい副題を表示
- `group` が無いが `role=parent` がある形式では、見出しモード上部へ `親タイムスタンプ` カード一覧を追加
- 親カードへ開始時刻と子項目数を表示
- 親カードを押すと該当時刻から再生
- 親だけ存在する場合の案内文を `大見出しはありません / 親タイムスタンプから直接ジャンプできます` へ変更

これにより `▷親 / ┗子` や `└子` 混在形式でも、解析結果の親子関係を表示側で確認しやすくした。

#### キーボード操作

追加:

```text
Space / K   再生・一時停止
J           10秒戻る
L           10秒進む
```

既存:

```text
Ctrl + K    検索
?           ヘルプ
Esc         スマホサイドバーを閉じる
```

入力欄・select・textarea・contenteditable・dialog表示中は再生ショートカットを無効化。

#### アクセシビリティ

- Toastへ `role=status / aria-live=polite / aria-atomic=true`
- 検索 / シーク / 音量へaria-labelを追加
- song / Dashboard / chapter / timestampのfocus-visibleを強化
- `prefers-reduced-motion` を尊重

### 新規ファイル

- `app-quality-v21.js`
- `quality-v21.css`
- `timestamp-polish-v21.js`

### 変更したファイル

- `index.html`
- `README.md`
- `作業報告書.md`

### 保存互換性

変更なし。

```text
asmrtube.library.v1
asmrtube.settings.v1
asmrtube.timestamp.view.v1
asmrtube.snapshot.v1
asmrtube.snapshot.beforeRestore.v1
```

v2.1は既存保存形式を変更せず、既存機能の外側に品質レイヤーを追加している。

### 崩していない仕様

- GitHub Pages静的構成
- YouTube IFrame Player
- YouTube Data APIキー不要
- v1.9タイムスタンプパーサー
- v1.8タイムスタンプ表示モード
- v2.0 Dashboard / 設定 / データ管理 / スマホドロワー
- JSON書き出し
- 既存localStorageキー

### 確認したこと

- `index.html` から `quality-v21.css` / `app-quality-v21.js` / `timestamp-polish-v21.js` を読み込む構成にした
- GitHub Pages用の相対パスを維持
- README / 作業報告書をv2.1へ更新
- v1.9パーサー本体と保存キーは変更していない

### 未確認

- GitHub Pages上でv2.1全操作の実ブラウザ確認
- YouTube API準備待ちの実通信タイミング確認
- UndoのiPhone / Android実機タップ確認
- 親タイムスタンプカードのスマホ実機確認
- 大規模ライブラリでタイムスタンプ全文検索した時の体感性能
- 長時間連続再生
- 埋め込み禁止動画の詳細エラー表示

## 2026-08-29 v2.0 他プロジェクト設計の横展開

### 主な変更

- LyricTubeからスマホドロワー、設定、ヘルプ、表示密度設定を応用
- Lineup Labから情報階層、データ管理、軽量な高品質UI方針を応用
- VReviewからDashboard、状態表示、Confidenceの考え方を応用
- ライブラリ概要Dashboard追加
- 明るさ / コンパクト / サムネ / 動き抑制 / 起動画面設定
- ヘルプダイアログ
- データ管理ダイアログ
- ローカルスナップショット
- タイムスタンプ解析Confidence表示
- 900px以下のスマホドロワー
- CSSを `ui-base-v2.css / product-v2.css` へ分離
- favicon追加

## 2026-08-29 v1.9 汎用タイムスタンプ解析

### 主な変更

- YouTube公式チャプター仕様と実際のコメント記法を調査
- 多段階パーサーへ変更
- Markdown / URL / 括弧時刻 / ハイフン / 1行崩れ / 親子記号 / YouTube絵文字 / 二言語へ対応
- `role / parentTime / parentLabel / confidence / sourceStyle / subtitle` を任意情報として追加
- `tests/timestamp-cases.json`
- `tests/timestamp-parser.test.html`
- 代表10形式の回帰ケースを追加

## 2026-08-29 v1.8 タイムスタンプ表示再設計

- `見出し / すべて` の2モード
- 見出しカード
- 親の先頭シーク
- 再生中見出しへ自動追従
- 親グループ折りたたみ
- タイムスタンプ一覧をコンパクト化

## 2026-08-29 v1.7 見出しジャンプ

- 親グループ一覧
- 親先頭への動画シーク
- 同名親を開始時刻で区別
- 再生中親の強調

## 2026-08-29 v1.6 見出し判定安定化

- `右 / 左` 等の子要素が続く場合だけ見出し化
- グループ終了条件を明確化
- グループ専用ブロック表示

## 2026-08-29 v1.5 1行崩れコメント対応

- タイムスタンプトークン単位の分解
- 2個以上の空白を論理区切りとして利用
- 再取り込み時の同時刻置換

## 2026-08-29 v1.4 タイムスタンプ構造・表示設定

- `group` 保存
- 画面明るさ設定
- サムネイル付き現在作品表示
- `asmrtube.settings.v1`

## 2026-08-29 v1.3 見出し保持試作

- 見出し文字列連結方式を追加
- v1.4でgroup方式へ置換

## 2026-08-29 v1.2 登録自動化

- YouTube URLからタイトル / チャンネル名取得
- oEmbed / noembed
- タイムスタンプパネル小型化
- 再生中タイムスタンプ強調

## 2026-08-27 v1.1 LyricTube UI寄せ

- 左サイドバー / 中央Player / 右タイムスタンプ
- LyricTube系デザインシステムへ統一

## 2026-08-27 v1 初期作成

- `EliteMay/asmrtube` として分離
- GitHub Pages静的構成
- ライブラリ / 検索 / タグ / お気に入り / 履歴 / 睡眠向け / プレイリスト
- コメントタイムスタンプ抽出
- A-Bリピート
- スリープタイマー
- JSONバックアップ
- 動画ID重複防止
