# ASMRTube 作業報告書

## 2026-08-29 v1.2 登録自動化・タイムスタンプ操作性改善

### 今回変更した内容

- YouTube URL貼り付け時に動画タイトルを自動取得
- YouTube URL貼り付け時にチャンネル名を自動取得
- YouTube oEmbedを第一候補として使用
- oEmbed取得失敗時にnoembedへフォールバック
- APIキー不要のまま自動取得を実現
- 自動取得後もタイトル / 配信者を手動修正可能
- 登録画面へ「情報取得」ボタンと取得状態表示を追加
- 保存時にタイトル / 配信者が空なら自動取得を再試行
- `/live/` 形式のYouTube URL認識を追加
- タイムスタンプ右パネルを固定的に長く伸ばさない構成へ変更
- タイムスタンプパネルの高さを約300〜480pxへ制限
- 長いタイムスタンプ一覧はパネル内スクロールへ変更
- 各タイムスタンプを「時間 + 内容 + 削除」のコンパクト1行表示へ変更
- 通常一覧ではタグと番号を非表示にして情報量を整理
- 時間だけでなく内容部分をクリックしても該当位置へジャンプ可能に変更
- 現在再生中のタイムスタンプ行を自動ハイライト
- タイムスタンプ件数表示を追加
- ASMR固有CSSを `asmr-overrides.css` に分離

### 変更したファイル

- `index.html`
- `app.js`
- `asmr-overrides.css`（新規）
- `README.md`
- `作業報告書.md`

### 崩していない仕様

- `localStorage` キーは `asmrtube.library.v1` のまま
- 既存の保存データ形式を変更していない
- LyricTubeの `lyrictube.library.v3` へ干渉しない
- GitHub Pagesだけで動作する静的構成
- YouTube Data APIキー不要
- YouTube IFrame Playerを利用
- YouTube動画IDによる重複登録防止
- JSONバックアップ / 復元
- コメント貼り付けによるタイムスタンプ解析

### 確認項目

- `index.html` から `styles.css` → `asmr-overrides.css` の順で読み込む構成
- `app.js` は `v=3` としてキャッシュ更新対象に変更
- タイトル / チャンネル名の自動取得は登録データ形式を変更しない
- タイムスタンプの保存形式も変更しない

### 未確認 / 今後必要な作業

- GitHub Pages実ブラウザ上でYouTube oEmbedのCORS通信を複数動画で確認
- 年齢制限 / 非公開 / 削除済み等の動画で自動取得失敗時の表示確認
- 実際の長いタイムスタンプコメントでスクロール操作を確認
- iPhone / Android実機でタイムスタンプパネル高さを確認
- 埋め込み禁止動画のエラー表示改善

## 2026-08-27 v1.1 LyricTube UI寄せ

- ASMRTubeの画面構成をLyricTubeに近い構成へ変更
- 左サイドバーをLyricTube方式に変更
- 中央をLyricTubeのプレイヤー列に近い構成へ変更
- LyricTubeの歌詞パネルに相当する右カラムを、ASMRタイムスタンプパネルとして実装
- 配色、余白、角丸、ボタン、パネル、ダイアログ等のCSSをLyricTubeの必要部分を基準に再構成
- ASMRTubeで不要な歌詞専用CSSは流用しない方針へ変更
- 既存のASMR登録、タグ、評価、音量、プレイリスト、履歴、コメント貼り付け解析などは維持

## 2026-08-27 v1 初期作成

- LyricTubeとは別の `EliteMay/asmrtube` リポジトリへASMRTubeを分離
- GitHub Pagesでルート公開できる静的構成を作成
- ASMRライブラリ、検索、タグ絞り込み、お気に入り、履歴、睡眠向け一覧、プレイリストを追加
- 作品ごとの評価と音量保存を追加
- YouTubeコメント貼り付けによるタイムスタンプ抽出を追加
- `m:ss` / `mm:ss` / `h:mm:ss` に対応
- タイムスタンプ名からタグ候補を自動付与
- タイムスタンプから指定位置へシーク
- A-B区間リピート
- スリープタイマー
- JSONバックアップ / 復元
- YouTube動画IDによる重複登録防止
