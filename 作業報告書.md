# ASMRTube 作業報告書

## 2026-08-29 v1.5 改行崩れコメント解析・重複修正

### 今回変更した内容

- YouTubeコメントをコピーした際に改行が潰れ、1行の中へ複数タイムスタンプと見出しが混在する形式へ対応
- コメント全体をタイムスタンプトークン単位で分解する方式へ変更
- `2:41 炭酸  耳ふー 4:46 右 5:38 左` のような形式を、`2:41 炭酸` と `耳ふー > 4:46 右 / 5:38 左` に分離
- `7:13 喋りながらの移動  綿棒 10:48 右 12:30 左  指耳かき 14:02 左` のように複数グループが連続する形式へ対応
- 改行または2個以上の空白を論理的な区切りとして扱う処理を追加
- Markdownリンク内のURLを除去し、表示テキストだけを解析へ利用
- 再取り込み時、解析された時刻と同じ既存タイムスタンプを置き換えるように変更
- 旧パーサーで作られた同時刻の重複を、元コメントを再取り込みすることで修復可能にした
- `timestamp-parser.js` / `timestamp-ui.js` のキャッシュバージョンをv1.5へ更新

### 修正した不具合

- `4:46 右` と `4:46 耳ふー右` のような同時刻重複
- `5:55 耳塞ぎ` まで直前の `耳ふー` 見出しを誤継承する問題
- `綿棒` / `指耳かき` など、前のタイムスタンプ行の末尾に埋め込まれた次グループ見出しを正しく判定できない問題
- コメントが1行へ連結されるとグループ構造を失う問題

### 確認したこと

ユーザー提示コメントの主要部分と同じ構造をJavaScriptでテストし、次の解析結果になることを確認した。

```text
2:41 炭酸
耳ふー
 4:46 右
 5:38 左
5:55 耳塞ぎ
7:13 喋りながらの移動
綿棒
10:48 右
12:30 左
指耳かき
14:02 左
14:07 右
耳ふー
14:34 左
14:54 右
15:35 よしよし
```

- 既存ライブラリ保存キー `asmrtube.library.v1` は変更していない
- `group` は任意項目のままで既存データとの互換性を維持
- 同時刻置換はタイムスタンプ再取り込み時だけ実行し、通常表示時に既存データを勝手に削除しない

### 未確認 / 今後必要な作業

- GitHub Pages反映後にユーザー提示のコメント全文を実ブラウザへ貼り付けた最終表示確認
- 他チャンネルの異なるコメント書式での見出し判定精度
- 必要なら「全タイムスタンプを再解析して置換」専用ボタンを追加

## 2026-08-29 v1.4 タイムスタンプ構造修正・画面設定・UI品質改善

### 今回変更した内容

- v1.3の「見出しを各行へ文字列連結する方式」を廃止
- タイムスタンプへ任意の `group` を持たせ、見出しをグループとして保持する方式へ変更
- `耳ふー` の下へ `4:46 右 / 5:38 左` のように表示できるように変更
- `[4:46](YouTubeリンク) 右` のMarkdown形式を継続対応
- クリップボードで `4:46` と `右` が別行へ分割された場合も補完
- 保存前プレビューを `見出し / 時間 / 内容` の3項目へ変更
- 見出し込みでタグ候補を判定
- グループ見出しをタイムスタンプ一覧へ1回だけ表示
- 既存の `{time,label,tags}` データはそのまま表示可能で、`group` は任意項目として追加
- タイムスタンプ保存時の重複判定へ `group` を追加
- `timestamp-parser.js` のキャッシュバージョンを更新
- 画面全体の明るさ設定を追加
- 明るさを30〜100%で調整可能にした
- 100 / 75 / 55 / 35%のプリセットを追加
- 明るさ設定を `asmrtube.settings.v1` へ保存し、ライブラリデータと分離
- 左上へ設定ボタンを追加
- ヘッダーへ現在選択中作品のサムネイルを追加
- ブランドマークを簡易波形アイコンへ変更
- サイドバーへバージョン表示を追加
- ヘッダー、カード、パネルの陰影と階層感を調整しLyricTube系の製品UIへ寄せた

### 変更したファイル

- `timestamp-parser.js`
- `timestamp-ui.js`（新規）
- `ui-enhancements.js`（新規）
- `index.html`
- `README.md`
- `作業報告書.md`

### 保存仕様

- ライブラリ: `asmrtube.library.v1`
- 表示設定: `asmrtube.settings.v1`
- 既存ライブラリの保存キーは変更していない
- 既存タイムスタンプの `group` 未設定データもそのまま利用可能

### 修正した不具合

- 見出しを各タイムスタンプ名へ強制的に連結することでコメント構造が崩れる問題を修正
- コピー形式によって時刻と左右表記が別行になる場合に、見出しや左右情報が欠落する問題へ対応
- パーサー更新後もブラウザキャッシュで旧版が残る可能性があったため読み込みバージョンを更新

### 確認したこと

- GitHub Pagesで利用できる相対パス構成を維持
- `group` は追加フィールドのため既存データを破壊しない
- 明るさ設定はライブラリ保存データへ混在しない
- 追加UIは既存のYouTube IFrame Playerやタイムスタンプ保存キーを変更しない

### 未確認 / 今後必要な作業

- GitHub Pagesへ最新コミット反映後の実ブラウザで、ユーザー提示の実コメントを再度貼り付けて表示確認
- iPhone / Android実機で設定ダイアログと明るさオーバーレイを確認
- 多数のグループを含む長いタイムスタンプでスクロール操作を確認
- 必要に応じてグループ見出しの手動追加 / 並び替え機能を追加

## 2026-08-29 v1.3 タイムスタンプ見出し保持

- タイムスタンプ直前の短い見出しを各ラベルへ連結する方式を追加
- Markdownリンク形式へ対応
- `耳ふー` と左右表記を認識
- v1.4で、連結方式は操作性と構造保持の問題があったためグループ方式へ置き換え

## 2026-08-29 v1.2 登録自動化・タイムスタンプ操作性改善

- YouTube URL貼り付け時に動画タイトル / チャンネル名を自動取得
- YouTube oEmbedを第一候補、noembedをフォールバックとして使用
- APIキー不要のまま自動取得
- 自動取得後も手動修正可能
- `/live/` 形式のYouTube URL認識を追加
- タイムスタンプパネルを約300〜480pxへ制限
- 長い一覧はパネル内スクロール
- 各行を「時間 + 内容 + 削除」のコンパクト表示へ変更
- 内容部分クリックでも該当位置へジャンプ可能
- 現在再生中のタイムスタンプを自動ハイライト
- タイムスタンプ件数表示を追加
- ASMR固有CSSを `asmr-overrides.css` に分離

## 2026-08-27 v1.1 LyricTube UI寄せ

- ASMRTubeの画面構成をLyricTubeに近い構成へ変更
- 左サイドバーをLyricTube方式に変更
- 中央をLyricTubeのプレイヤー列に近い構成へ変更
- LyricTubeの歌詞パネルに相当する右カラムを、ASMRタイムスタンプパネルとして実装
- 配色、余白、角丸、ボタン、パネル、ダイアログ等のCSSをLyricTubeの必要部分を基準に再構成
- ASMRTubeで不要な歌詞専用CSSは流用しない方針へ変更
- 既存のASMR登録、タグ、評価、音量、プレイリスト、履歴、コメント貼り付け解析などは維持

## 2026-08-27 v1 初期作成

- LyricTubeとは別の `EliteMay/asmrtube` リポジトリへASMRTubeを分離
- GitHub Pagesでルート公開できる静的構成を作成
- ASMRライブラリ、検索、タグ絞り込み、お気に入り、履歴、睡眠向け一覧、プレイリストを追加
- 作品ごとの評価と音量保存を追加
- YouTubeコメント貼り付けによるタイムスタンプ抽出を追加
- `m:ss` / `mm:ss` / `h:mm:ss` に対応
- タイムスタンプ名からタグ候補を自動付与
- タイムスタンプから指定位置へシーク
- A-B区間リピート
- スリープタイマー
- JSONバックアップ / 復元
- YouTube動画IDによる重複登録防止
